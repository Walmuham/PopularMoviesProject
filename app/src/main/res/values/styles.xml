<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="style_poster_gridview" parent="android:Widget.GridView">
        <item name="android:columnWidth">@dimen/poster_thump_w</item>


        <item name="android:numColumns">auto_fit</item>

        <item name="android:verticalSpacing">@dimen/poster_thump_s</item>
        <item name="android:horizontalSpacing">@dimen/poster_thump_s</item>
        <item name="android:padding">@dimen/poster_thump_s</item>
        <item name="android:listSelector">@drawable/selector_gridview</item>


    </style>

</resources>
